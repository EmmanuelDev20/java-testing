import java.io.*;

// public class Plantilla {
//     public static void main(String[] args) {
//         try {
//             BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
//             String linea;
//             while ((linea = br.readLine()) != null) {
//                 System.out.println(linea);
//             }
//         } catch (IOException e) {
//             e.printStackTrace();
//         }
//     }
// }

public class Plantilla {
  public static void main(String[] args) throws IOException {
    // int number = (int) (0.2 * 0.13);
    // int number = Integer.parseInt(100 * 0.13);

    // int number = Integer.parseInt(100 * (13 / 100));
    int number = (int) (100 * (13 / 100.0));

    // int number = (100 * 0.13 * 100);
    System.out.println(number);
    // BufferedReader in = new BufferedReader(new InputStreamReader(System.in));

    // String nombre;
    // nombre = "Enrique VII";

    // String puesto = "Su majestad el rey";

    // System.out.println(puesto);
    // System.out.println(nombre);
    // System.out.println(puesto);

    // // Puedo declarar dos variables del mismo tipo a la vez.
    // String nombreReina, puestoReina;
    // // Solicitar la información al usuario.
    // System.out.println("Digite el nombre de la esposa del Rey");
    // nombreReina = in.readLine();
    // System.out.println("Digite el puesto de la Reina");
    // puestoReina = in.readLine();
    // // todo: imprimir el nombre de la Reina y el puesto de la Reina.
    // System.out.println(nombreReina + " " + puestoReina);
    // // todo:  agregar variable edad, para ambos y solicitar al usuario. La edad debe ser un entero.
    // int edadRey, edadReina;
    // // todo: Encontrar la diferencia entre las 2 edades
    // System.out.println("Digite la edad del Rey");
    // edadRey = Integer.parseInt(in.readLine());
    // System.out.println("Digite la edad de la Reina");
    // edadReina = Integer.parseInt(in.readLine());
    // System.out.println("La diferencia de edades es: " + (edadRey - edadReina));
  }
}